// ternary operator

// (condition) ? if condtion is true this code will work : this will get execute if condition is false
// condtion ? true : false


let age = 19
let permission  = age >= 18 ? "allowed" : "not allowed"

// console.log(permission)

// if(age >= 18){
//     console.log("allowed")
// } else {
//     console("not allowed")
// }

 
let add = 4 + 4

let result = add && "write answer" 
let result2 = add || "write answer"
 
